{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "Condition": {
      "additionalProperties": false,
      "description": "Status condition entry.",
      "properties": {
        "lastTransitionTime": {
          "description": "Last transition timestamp.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "message": {
          "description": "Human-readable message.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "description": "Machine-readable reason code.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Status value (\"True\", \"False\", \"Unknown\").",
          "type": "string"
        },
        "type": {
          "description": "Type of condition (e.g. Reconciled, Ready).",
          "type": "string"
        }
      },
      "required": [
        "status",
        "type"
      ],
      "type": "object"
    },
    "Credential": {
      "additionalProperties": false,
      "description": "Declared credential for a service account (PF-36, PF-37).\n\n**Security note**: The security property of this kind is the complete absence of secret fields. `Credential` has nowhere to put a secret and carries `deny_unknown_fields`, so `secret:`, `value:`, `password:`, `clientSecret:` and `apiKey:` are all rejected at parse time (PF-36). The key id and the Argon2id hash are stored in the database / Keycloak / secret store, never in manifest state or Git.",
      "properties": {
        "expiresAt": {
          "description": "Optional expiration timestamp (UTC).",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "ipAllowList": {
          "description": "Whitelist of allowed client IP CIDR blocks (PF-36).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/CredentialKind"
            }
          ],
          "description": "Type of credential."
        },
        "name": {
          "description": "Credential identifier name (DNS-1123 label).",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "name"
      ],
      "type": "object"
    },
    "CredentialKind": {
      "description": "Category of service account credential.",
      "oneOf": [
        {
          "description": "Keycloak confidential OAuth 2.1 client with client_credentials flow.",
          "enum": [
            "oauth-client"
          ],
          "type": "string"
        },
        {
          "description": "Static hashed API bearer key for external legacy systems (PF-36, PF-37).",
          "enum": [
            "api-key"
          ],
          "type": "string"
        }
      ]
    },
    "KubernetesBinding": {
      "additionalProperties": false,
      "description": "Kubernetes ServiceAccount binding coordinates (PF-47).",
      "properties": {
        "namespace": {
          "description": "Kubernetes namespace name.",
          "type": "string"
        },
        "serviceAccount": {
          "description": "Kubernetes ServiceAccount name.",
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "serviceAccount"
      ],
      "type": "object"
    },
    "MultiLanguageMap": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Multi-language map keyed by ISO 639-1 two-letter lowercase language codes",
      "propertyNames": {
        "pattern": "^[a-z]{2}$",
        "type": "string"
      },
      "type": "object"
    },
    "ObjectMeta": {
      "additionalProperties": false,
      "description": "Metadata envelope attached to every platform resource (MF-02).",
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value annotations for provenance and ownership (MF-08).",
          "type": "object"
        },
        "description": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual description (PF-24)."
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value labels for filtering (MF-10).",
          "type": "object"
        },
        "name": {
          "description": "Resource name (DNS-1123 label).",
          "type": "string"
        },
        "namespace": {
          "description": "Resource namespace (\"org\" or project slug).",
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual title (PF-24)."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Operation": {
      "description": "Table 4.20-1: every named distributed API operation in ETSI GS CIM 009 (R8).\n\nClosed enum ensures no proprietary verbs (`upsertEntity`, `read`, `write`) are accepted, satisfying the strict R8 CIM 009 vocabulary guarantee.",
      "oneOf": [
        {
          "description": "Create entity (`POST /entities`).",
          "enum": [
            "createEntity"
          ],
          "type": "string"
        },
        {
          "description": "Update entity (`PATCH /entities/{id}`).",
          "enum": [
            "updateEntity"
          ],
          "type": "string"
        },
        {
          "description": "Append entity attributes (`POST /entities/{id}/attrs`).",
          "enum": [
            "appendAttrs"
          ],
          "type": "string"
        },
        {
          "description": "Update entity attributes (`PATCH /entities/{id}/attrs`).",
          "enum": [
            "updateAttrs"
          ],
          "type": "string"
        },
        {
          "description": "Delete entity attributes (`DELETE /entities/{id}/attrs/{attr}`).",
          "enum": [
            "deleteAttrs"
          ],
          "type": "string"
        },
        {
          "description": "Delete entity (`DELETE /entities/{id}`).",
          "enum": [
            "deleteEntity"
          ],
          "type": "string"
        },
        {
          "description": "Batch create entities (`POST /entityOperations/create`).",
          "enum": [
            "createBatch"
          ],
          "type": "string"
        },
        {
          "description": "Batch upsert entities (`POST /entityOperations/upsert`).",
          "enum": [
            "upsertBatch"
          ],
          "type": "string"
        },
        {
          "description": "Batch update entities (`POST /entityOperations/update`).",
          "enum": [
            "updateBatch"
          ],
          "type": "string"
        },
        {
          "description": "Batch delete entities (`POST /entityOperations/delete`).",
          "enum": [
            "deleteBatch"
          ],
          "type": "string"
        },
        {
          "description": "Upsert temporal entity attributes (`POST /temporal/entities/{id}/attrs`).",
          "enum": [
            "upsertTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Append temporal entity attributes (`POST /temporal/entities/{id}/attrs`).",
          "enum": [
            "appendAttrsTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Delete temporal entity attributes (`DELETE /temporal/entities/{id}/attrs/{attr}`).",
          "enum": [
            "deleteAttrsTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Update temporal attribute instance (`PATCH /temporal/entities/{id}/attrs/{attr}/{instance}`).",
          "enum": [
            "updateAttrInstanceTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Delete temporal attribute instance (`DELETE /temporal/entities/{id}/attrs/{attr}/{instance}`).",
          "enum": [
            "deleteAttrInstanceTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Delete temporal entity (`DELETE /temporal/entities/{id}`).",
          "enum": [
            "deleteTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Merge entity (`PATCH /entities/{id}`).",
          "enum": [
            "mergeEntity"
          ],
          "type": "string"
        },
        {
          "description": "Replace entity (`PUT /entities/{id}`).",
          "enum": [
            "replaceEntity"
          ],
          "type": "string"
        },
        {
          "description": "Replace entity attributes (`PATCH /entities/{id}/attrs`).",
          "enum": [
            "replaceAttrs"
          ],
          "type": "string"
        },
        {
          "description": "Batch merge entities (`POST /entityOperations/merge`).",
          "enum": [
            "mergeBatch"
          ],
          "type": "string"
        },
        {
          "description": "Purge entity permanently (`DELETE /entities/{id}?purge=true`).",
          "enum": [
            "purgeEntity"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve single entity (`GET /entities/{id}`).",
          "enum": [
            "retrieveEntity"
          ],
          "type": "string"
        },
        {
          "description": "Query entities (`GET /entities` or `POST /entityOperations/query`).",
          "enum": [
            "queryEntity"
          ],
          "type": "string"
        },
        {
          "description": "Batch query entities (`POST /entityOperations/query`).",
          "enum": [
            "queryBatch"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve temporal entity (`GET /temporal/entities/{id}`).",
          "enum": [
            "retrieveTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Query temporal entities (`GET /temporal/entities`).",
          "enum": [
            "queryTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve entity types list (`GET /types`).",
          "enum": [
            "retrieveEntityTypes"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve entity type details (`GET /types/{type}`).",
          "enum": [
            "retrieveEntityTypeDetails"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve entity type info (`GET /types/{type}/info`).",
          "enum": [
            "retrieveEntityTypeInfo"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve attribute types list (`GET /attributes`).",
          "enum": [
            "retrieveAttrTypes"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve attribute type details (`GET /attributes/{attr}`).",
          "enum": [
            "retrieveAttrTypeDetails"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve attribute type info (`GET /attributes/{attr}/info`).",
          "enum": [
            "retrieveAttrTypeInfo"
          ],
          "type": "string"
        },
        {
          "description": "Create context subscription (`POST /subscriptions`).",
          "enum": [
            "createSubscription"
          ],
          "type": "string"
        },
        {
          "description": "Update context subscription (`PATCH /subscriptions/{id}`).",
          "enum": [
            "updateSubscription"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve context subscription (`GET /subscriptions/{id}`).",
          "enum": [
            "retrieveSubscription"
          ],
          "type": "string"
        },
        {
          "description": "Query context subscriptions (`GET /subscriptions`).",
          "enum": [
            "querySubscription"
          ],
          "type": "string"
        },
        {
          "description": "Delete context subscription (`DELETE /subscriptions/{id}`).",
          "enum": [
            "deleteSubscription"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve context entity map (`GET /entityMaps/{id}`).",
          "enum": [
            "retrieveEntityMap"
          ],
          "type": "string"
        },
        {
          "description": "Update context entity map (`PATCH /entityMaps/{id}`).",
          "enum": [
            "updateEntityMap"
          ],
          "type": "string"
        },
        {
          "description": "Delete context entity map (`DELETE /entityMaps/{id}`).",
          "enum": [
            "deleteEntityMap"
          ],
          "type": "string"
        },
        {
          "description": "Create entity map query on entities.",
          "enum": [
            "createEntityMapQueryEntity"
          ],
          "type": "string"
        },
        {
          "description": "Create entity map query on temporal entities.",
          "enum": [
            "createEntityMapQueryTemporal"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve context source identity (`GET /csourceIdentity`).",
          "enum": [
            "retrieveContextSourceIdentity"
          ],
          "type": "string"
        }
      ]
    },
    "OperationGroup": {
      "description": "Table 4.20-2: the five distributed operation group names (R8).",
      "oneOf": [
        {
          "description": "Consumption, subscription, and entity map operations.",
          "enum": [
            "federationOps"
          ],
          "type": "string"
        },
        {
          "description": "Association operations without entity map support.",
          "enum": [
            "associationOps"
          ],
          "type": "string"
        },
        {
          "description": "Entity and attribute update operations.",
          "enum": [
            "updateOps"
          ],
          "type": "string"
        },
        {
          "description": "Retrieve and query entity operations.",
          "enum": [
            "retrieveOps"
          ],
          "type": "string"
        },
        {
          "description": "Full redirection operations.",
          "enum": [
            "redirectionOps"
          ],
          "type": "string"
        }
      ]
    },
    "OperationRef": {
      "anyOf": [
        {
          "allOf": [
            {
              "$ref": "#/definitions/Operation"
            }
          ],
          "description": "Individual named operation from CIM 009 Table 4.20-1."
        },
        {
          "allOf": [
            {
              "$ref": "#/definitions/OperationGroup"
            }
          ],
          "description": "Operation group from CIM 009 Table 4.20-2."
        }
      ],
      "description": "Reference to a CIM 009 operation or operation group (R8).\n\nClosed enum ensures no proprietary verbs (`read`, `write`, `upsertEntity`) are permitted, satisfying the strict R8 CIM 009 vocabulary guarantee."
    },
    "Owner": {
      "additionalProperties": false,
      "description": "Accountable human owner reference (PF-34).",
      "properties": {
        "user": {
          "description": "Identifier or username of the accountable human user.",
          "type": "string"
        }
      },
      "required": [
        "user"
      ],
      "type": "object"
    },
    "Phase": {
      "description": "Lifecycle phase enumeration.",
      "oneOf": [
        {
          "description": "Resource manifest is in draft state.",
          "enum": [
            "Draft"
          ],
          "type": "string"
        },
        {
          "description": "Resource is pending review or approval.",
          "enum": [
            "Pending"
          ],
          "type": "string"
        },
        {
          "description": "Resource deployment is in progress.",
          "enum": [
            "Deploying"
          ],
          "type": "string"
        },
        {
          "description": "Resource is live and active in cluster.",
          "enum": [
            "Live"
          ],
          "type": "string"
        },
        {
          "description": "Reconciliation error.",
          "enum": [
            "Error"
          ],
          "type": "string"
        }
      ]
    },
    "RoleBinding": {
      "additionalProperties": false,
      "description": "Scoped role grant assignment.",
      "properties": {
        "operations": {
          "description": "CIM 009 operations or operation groups granted (R8).",
          "items": {
            "$ref": "#/definitions/OperationRef"
          },
          "type": "array"
        },
        "role": {
          "description": "Role template name (DNS-1123 label).",
          "type": "string"
        },
        "scope": {
          "allOf": [
            {
              "$ref": "#/definitions/RoleScope"
            }
          ],
          "description": "Target hierarchical scope of this grant."
        },
        "types": {
          "description": "Optional entity types whitelist.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "role",
        "scope"
      ],
      "type": "object"
    },
    "RoleScope": {
      "additionalProperties": false,
      "description": "Target scope of a role grant: exactly one of `contextSpace`, `project`, or `organization`.",
      "properties": {
        "contextSpace": {
          "description": "Context Space scope target.",
          "type": [
            "string",
            "null"
          ]
        },
        "organization": {
          "description": "Organization scope target.",
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Project scope target.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ServiceAccountLimits": {
      "additionalProperties": false,
      "description": "Rate limiting bounds for a service account.",
      "properties": {
        "requestsPerMinute": {
          "description": "Maximum allowed requests per minute.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "requestsPerMinute"
      ],
      "type": "object"
    },
    "ServiceAccountSpec": {
      "additionalProperties": false,
      "description": "Desired specification of a [`ServiceAccount`][crate::kinds::ServiceAccount] resource (PF-34, PF-35, PF-47).",
      "properties": {
        "credentials": {
          "description": "Authentication credentials registered for this identity.",
          "items": {
            "$ref": "#/definitions/Credential"
          },
          "type": "array"
        },
        "limits": {
          "anyOf": [
            {
              "$ref": "#/definitions/ServiceAccountLimits"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional rate limiting configuration."
        },
        "owner": {
          "allOf": [
            {
              "$ref": "#/definitions/Owner"
            }
          ],
          "description": "Accountable human owner of this service identity (PF-34)."
        },
        "purpose": {
          "description": "Reason why this identity exists, shown in audit reviews.",
          "type": "string"
        },
        "roles": {
          "description": "Scoped platform role bindings granted to this identity.",
          "items": {
            "$ref": "#/definitions/RoleBinding"
          },
          "type": "array"
        },
        "workload": {
          "anyOf": [
            {
              "$ref": "#/definitions/Workload"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional Kubernetes workload binding (PF-47)."
        }
      },
      "required": [
        "credentials",
        "owner",
        "purpose",
        "roles"
      ],
      "type": "object"
    },
    "Status": {
      "additionalProperties": false,
      "description": "Server-computed lifecycle and reconciliation status (MF-04). Never stored in Git.",
      "properties": {
        "conditions": {
          "description": "Reconciler condition transitions.",
          "items": {
            "$ref": "#/definitions/Condition"
          },
          "type": "array"
        },
        "observedRevision": {
          "description": "Git commit revision observed during reconciliation.",
          "type": [
            "string",
            "null"
          ]
        },
        "phase": {
          "anyOf": [
            {
              "$ref": "#/definitions/Phase"
            },
            {
              "type": "null"
            }
          ],
          "description": "Current lifecycle phase."
        }
      },
      "type": "object"
    },
    "Workload": {
      "additionalProperties": false,
      "description": "Cluster workload binding container (PF-47).",
      "properties": {
        "kubernetes": {
          "allOf": [
            {
              "$ref": "#/definitions/KubernetesBinding"
            }
          ],
          "description": "Kubernetes pod workload binding."
        }
      },
      "required": [
        "kubernetes"
      ],
      "type": "object"
    }
  },
  "description": "Kubernetes-style resource envelope (MF-01..MF-04).",
  "properties": {
    "apiVersion": {
      "description": "API version (must match [`API_VERSION`]).",
      "type": "string"
    },
    "kind": {
      "description": "Resource kind name (must match [`Kind::KIND`]).",
      "type": "string"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/definitions/ObjectMeta"
        }
      ],
      "description": "Metadata identifying and labeling the resource."
    },
    "spec": {
      "allOf": [
        {
          "$ref": "#/definitions/ServiceAccountSpec"
        }
      ],
      "description": "Desired spec state."
    },
    "status": {
      "anyOf": [
        {
          "$ref": "#/definitions/Status"
        },
        {
          "type": "null"
        }
      ],
      "description": "Server-computed status. Stripped during export and serialization."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "ResourceEnvelope_for_ServiceAccountSpec",
  "type": "object"
}
