{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "Authorization": {
      "additionalProperties": false,
      "description": "An `Authorization` header whose value is assembled from a secret (CC-06).",
      "properties": {
        "header": {
          "description": "The header the credential is written into, `Authorization` when absent.\n\nAn open-data feed that authenticates with an API key usually wants it in a header of its own; without this the only way to reach such a feed would be to write the key into `headers` as a value, which is what MF-35 exists to prevent.",
          "type": [
            "string",
            "null"
          ]
        },
        "headerRef": {
          "allOf": [
            {
              "$ref": "#/definitions/SecretRef"
            }
          ],
          "description": "Where the credential comes from."
        },
        "scheme": {
          "description": "The scheme written before the secret.\n\n`Bearer` when absent on `Authorization`, and nothing at all on any other header: a vendor key header takes the bare key and refuses a scheme in front of it. An explicit scheme always wins.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "headerRef"
      ],
      "type": "object"
    },
    "Condition": {
      "additionalProperties": false,
      "description": "Status condition entry.",
      "properties": {
        "lastTransitionTime": {
          "description": "Last transition timestamp.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "message": {
          "description": "Human-readable message.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "description": "Machine-readable reason code.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Status value (\"True\", \"False\", \"Unknown\").",
          "type": "string"
        },
        "type": {
          "description": "Type of condition (e.g. Reconciled, Ready).",
          "type": "string"
        }
      },
      "required": [
        "status",
        "type"
      ],
      "type": "object"
    },
    "DataSourceSpec": {
      "additionalProperties": false,
      "description": "Desired state of a `DataSource`: one connection and its credentials (MF-35).",
      "properties": {
        "gtfsRt": {
          "anyOf": [
            {
              "$ref": "#/definitions/GtfsRtConnection"
            },
            {
              "type": "null"
            }
          ],
          "description": "The GTFS-realtime connection, present when `type` is `gtfs-rt`."
        },
        "http": {
          "anyOf": [
            {
              "$ref": "#/definitions/HttpConnection"
            },
            {
              "type": "null"
            }
          ],
          "description": "The HTTP connection, present when `type` is `http`."
        },
        "mqtt": {
          "anyOf": [
            {
              "$ref": "#/definitions/MqttConnection"
            },
            {
              "type": "null"
            }
          ],
          "description": "The MQTT connection, present when `type` is `mqtt`."
        },
        "tls": {
          "anyOf": [
            {
              "$ref": "#/definitions/TlsSettings"
            },
            {
              "type": "null"
            }
          ],
          "description": "Transport security for the connection above."
        },
        "type": {
          "allOf": [
            {
              "$ref": "#/definitions/DataSourceType"
            }
          ],
          "description": "Which connection block below is the live one."
        },
        "webSocket": {
          "anyOf": [
            {
              "$ref": "#/definitions/WebSocketConnection"
            },
            {
              "type": "null"
            }
          ],
          "description": "The WebSocket connection, present when `type` is `websocket`."
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    "DataSourceType": {
      "description": "Which kind of feed a [`DataSourceSpec`] connects to (MF-35).",
      "oneOf": [
        {
          "description": "An MQTT broker the runner subscribes to.",
          "enum": [
            "mqtt"
          ],
          "type": "string"
        },
        {
          "description": "An HTTP resource the runner polls.",
          "enum": [
            "http"
          ],
          "type": "string"
        },
        {
          "description": "A WebSocket the runner keeps open.",
          "enum": [
            "websocket"
          ],
          "type": "string"
        },
        {
          "description": "A GTFS-realtime protobuf feed, polled over HTTP and decoded by the runner.",
          "enum": [
            "gtfs-rt"
          ],
          "type": "string"
        }
      ]
    },
    "GtfsFeed": {
      "description": "The three feeds GTFS-realtime defines.",
      "oneOf": [
        {
          "description": "Where the vehicles are.",
          "enum": [
            "vehiclePositions"
          ],
          "type": "string"
        },
        {
          "description": "How the trips are running against the timetable.",
          "enum": [
            "tripUpdates"
          ],
          "type": "string"
        },
        {
          "description": "Service alerts.",
          "enum": [
            "alerts"
          ],
          "type": "string"
        }
      ]
    },
    "GtfsRtConnection": {
      "additionalProperties": false,
      "description": "A GTFS-realtime feed (Architecture/08 §6).",
      "properties": {
        "feed": {
          "allOf": [
            {
              "$ref": "#/definitions/GtfsFeed"
            }
          ],
          "description": "Which of the three GTFS-realtime feeds this URL serves."
        },
        "url": {
          "description": "The protobuf feed URL, `http://` or `https://`.",
          "type": "string"
        }
      },
      "required": [
        "feed",
        "url"
      ],
      "type": "object"
    },
    "HttpConnection": {
      "additionalProperties": false,
      "description": "An HTTP resource the runner polls (Architecture/08 §6).",
      "properties": {
        "authorization": {
          "anyOf": [
            {
              "$ref": "#/definitions/Authorization"
            },
            {
              "type": "null"
            }
          ],
          "description": "The `Authorization` header, built from a secret (CC-06)."
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Static request headers. A header carrying a credential belongs in `authorization`.",
          "type": "object"
        },
        "timeout": {
          "description": "Request timeout as a Bento duration, `10s`.",
          "type": [
            "string",
            "null"
          ]
        },
        "url": {
          "description": "The URL to fetch, `http://` or `https://`.",
          "type": "string"
        },
        "verb": {
          "description": "HTTP method, `GET` when absent.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "MqttConnection": {
      "additionalProperties": false,
      "description": "An MQTT connection (Architecture/08 §6).",
      "properties": {
        "cleanSession": {
          "description": "Whether the broker forgets the subscription between connections.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "passwordRef": {
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "Where the password comes from (CC-06)."
        },
        "qos": {
          "description": "Quality of service, 0, 1 or 2. Absent leaves Bento's default.",
          "format": "uint8",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "topics": {
          "description": "Topic filters to subscribe to; at least one.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "urls": {
          "description": "Broker URLs, `tcp://host:1883` or `tls://host:8883`; at least one.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "username": {
          "description": "The user the runner authenticates as; the password is a reference.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "topics",
        "urls"
      ],
      "type": "object"
    },
    "MultiLanguageMap": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Multi-language map keyed by ISO 639-1 two-letter lowercase language codes",
      "propertyNames": {
        "pattern": "^[a-z]{2}$",
        "type": "string"
      },
      "type": "object"
    },
    "ObjectMeta": {
      "additionalProperties": false,
      "description": "Metadata envelope attached to every platform resource (MF-02).",
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value annotations for provenance and ownership (MF-08).",
          "type": "object"
        },
        "description": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual description (PF-24)."
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value labels for filtering (MF-10).",
          "type": "object"
        },
        "name": {
          "description": "Resource name (DNS-1123 label).",
          "type": "string"
        },
        "namespace": {
          "description": "Resource namespace (\"org\" or project slug).",
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual title (PF-24)."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Phase": {
      "description": "Lifecycle phase enumeration.",
      "oneOf": [
        {
          "description": "Resource manifest is in draft state.",
          "enum": [
            "Draft"
          ],
          "type": "string"
        },
        {
          "description": "Resource is pending review or approval.",
          "enum": [
            "Pending"
          ],
          "type": "string"
        },
        {
          "description": "Resource deployment is in progress.",
          "enum": [
            "Deploying"
          ],
          "type": "string"
        },
        {
          "description": "Resource is live and active in cluster.",
          "enum": [
            "Live"
          ],
          "type": "string"
        },
        {
          "description": "Reconciliation error.",
          "enum": [
            "Error"
          ],
          "type": "string"
        }
      ]
    },
    "SecretRef": {
      "additionalProperties": false,
      "description": "Reference to a credential stored in SOPS or OpenBao (PF-36).\n\n`deny_unknown_fields` is a security control: it rejects inline `value:` or `password:` fields at parse time, enforcing that secrets never appear in Git or manifest files.",
      "properties": {
        "envVar": {
          "description": "Optional environment variable name for injection into runners.",
          "type": [
            "string",
            "null"
          ]
        },
        "key": {
          "description": "Optional key within secret.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Secret name in secret store.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Status": {
      "additionalProperties": false,
      "description": "Server-computed lifecycle and reconciliation status (MF-04). Never stored in Git.",
      "properties": {
        "conditions": {
          "description": "Reconciler condition transitions.",
          "items": {
            "$ref": "#/definitions/Condition"
          },
          "type": "array"
        },
        "observedRevision": {
          "description": "Git commit revision observed during reconciliation.",
          "type": [
            "string",
            "null"
          ]
        },
        "phase": {
          "anyOf": [
            {
              "$ref": "#/definitions/Phase"
            },
            {
              "type": "null"
            }
          ],
          "description": "Current lifecycle phase."
        }
      },
      "type": "object"
    },
    "TlsSettings": {
      "additionalProperties": false,
      "description": "How the runner trusts the feed's certificate.",
      "properties": {
        "caCertRef": {
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "A private certificate authority the feed's certificate chains to."
        },
        "insecureSkipVerify": {
          "description": "Kept so a manifest can state the choice; only `false` is accepted (see [`DataSourceSpec::validate`]).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "WebSocketConnection": {
      "additionalProperties": false,
      "description": "A WebSocket the runner keeps open (Architecture/08 §6).",
      "properties": {
        "openMessage": {
          "description": "A message sent once the socket opens, usually a subscription request.\n\nA socket that needs a credential authenticates inside this message: Bento's websocket input sends no `Authorization` header, and a field the renderer cannot honour would be a promise the runner breaks.",
          "type": [
            "string",
            "null"
          ]
        },
        "url": {
          "description": "The socket URL, `ws://` or `wss://`.",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  "description": "Kubernetes-style resource envelope (MF-01..MF-04).",
  "properties": {
    "apiVersion": {
      "description": "API version (must match [`API_VERSION`]).",
      "type": "string"
    },
    "kind": {
      "description": "Resource kind name (must match [`Kind::KIND`]).",
      "type": "string"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/definitions/ObjectMeta"
        }
      ],
      "description": "Metadata identifying and labeling the resource."
    },
    "spec": {
      "allOf": [
        {
          "$ref": "#/definitions/DataSourceSpec"
        }
      ],
      "description": "Desired spec state."
    },
    "status": {
      "anyOf": [
        {
          "$ref": "#/definitions/Status"
        },
        {
          "type": "null"
        }
      ],
      "description": "Server-computed status. Stripped during export and serialization."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "ResourceEnvelope_for_DataSourceSpec",
  "type": "object"
}
