{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "BundleOrigin": {
      "additionalProperties": false,
      "description": "A published bundle URL as a sync origin (MF-17).",
      "properties": {
        "secretRef": {
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "Credential for a protected bundle URL (MF-31)."
        },
        "url": {
          "description": "URL of the bundle archive or `kind: Bundle` index; `https://` only.",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "Condition": {
      "additionalProperties": false,
      "description": "Status condition entry.",
      "properties": {
        "lastTransitionTime": {
          "description": "Last transition timestamp.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "message": {
          "description": "Human-readable message.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "description": "Machine-readable reason code.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Status value (\"True\", \"False\", \"Unknown\").",
          "type": "string"
        },
        "type": {
          "description": "Type of condition (e.g. Reconciled, Ready).",
          "type": "string"
        }
      },
      "required": [
        "status",
        "type"
      ],
      "type": "object"
    },
    "ConflictPolicy": {
      "description": "What an import does when a resource already exists (MF-23).",
      "oneOf": [
        {
          "description": "Abort the whole run.",
          "enum": [
            "fail"
          ],
          "type": "string"
        },
        {
          "description": "Keep the local resource.",
          "enum": [
            "skip"
          ],
          "type": "string"
        },
        {
          "description": "Overwrite with the remote resource.",
          "enum": [
            "replace"
          ],
          "type": "string"
        },
        {
          "description": "Import the remote resource under a new name.",
          "enum": [
            "rename"
          ],
          "type": "string"
        }
      ]
    },
    "GitOrigin": {
      "additionalProperties": false,
      "description": "A remote Git repository as a sync origin.",
      "properties": {
        "path": {
          "description": "Subtree to sync; the whole repository when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "ref": {
          "description": "Branch, tag or commit to follow.",
          "type": "string"
        },
        "secretRef": {
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "Read-only credential, resolved by the reconciler (MF-31)."
        },
        "url": {
          "description": "Clone URL; `https://` or an `ssh://`/`git@` remote, never plain `http://`.",
          "type": "string"
        }
      },
      "required": [
        "ref",
        "url"
      ],
      "type": "object"
    },
    "MultiLanguageMap": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Multi-language map keyed by ISO 639-1 two-letter lowercase language codes",
      "propertyNames": {
        "pattern": "^[a-z]{2}$",
        "type": "string"
      },
      "type": "object"
    },
    "ObjectMeta": {
      "additionalProperties": false,
      "description": "Metadata envelope attached to every platform resource (MF-02).",
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value annotations for provenance and ownership (MF-08).",
          "type": "object"
        },
        "description": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual description (PF-24)."
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value labels for filtering (MF-10).",
          "type": "object"
        },
        "name": {
          "description": "Resource name (DNS-1123 label).",
          "type": "string"
        },
        "namespace": {
          "description": "Resource namespace (\"org\" or project slug).",
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual title (PF-24)."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Phase": {
      "description": "Lifecycle phase enumeration.",
      "oneOf": [
        {
          "description": "Resource manifest is in draft state.",
          "enum": [
            "Draft"
          ],
          "type": "string"
        },
        {
          "description": "Resource is pending review or approval.",
          "enum": [
            "Pending"
          ],
          "type": "string"
        },
        {
          "description": "Resource deployment is in progress.",
          "enum": [
            "Deploying"
          ],
          "type": "string"
        },
        {
          "description": "Resource is live and active in cluster.",
          "enum": [
            "Live"
          ],
          "type": "string"
        },
        {
          "description": "Reconciliation error.",
          "enum": [
            "Error"
          ],
          "type": "string"
        }
      ]
    },
    "PlatformApiOrigin": {
      "additionalProperties": false,
      "description": "Another platform instance's public resource API as a sync origin (MF-32).",
      "properties": {
        "baseUrl": {
          "description": "Base URL of the remote instance, `https://` only.",
          "type": "string"
        },
        "project": {
          "description": "Project slug to follow on the remote instance.",
          "type": "string"
        },
        "secretRef": {
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "Credential for the remote resource API (MF-31)."
        }
      },
      "required": [
        "baseUrl",
        "project"
      ],
      "type": "object"
    },
    "Schedule": {
      "additionalProperties": false,
      "description": "How often a [`SyncSourceSpec`] runs (MF-28): `{ interval: 30m }` or `{ webhook: true }`.\n\nExactly one of the two members is set, for the same reason as [`SyncOrigin`].",
      "properties": {
        "interval": {
          "description": "Fixed interval such as `30m`, `6h` or `1d` (suffixes `s`, `m`, `h`, `d`).",
          "type": [
            "string",
            "null"
          ]
        },
        "webhook": {
          "description": "Driven by a webhook from the source instead of a timer; only `true` is a schedule.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "SecretRef": {
      "additionalProperties": false,
      "description": "Reference to a credential stored in SOPS or OpenBao (PF-36).\n\n`deny_unknown_fields` is a security control: it rejects inline `value:` or `password:` fields at parse time, enforcing that secrets never appear in Git or manifest files.",
      "properties": {
        "envVar": {
          "description": "Optional environment variable name for injection into runners.",
          "type": [
            "string",
            "null"
          ]
        },
        "key": {
          "description": "Optional key within secret.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Secret name in secret store.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Status": {
      "additionalProperties": false,
      "description": "Server-computed lifecycle and reconciliation status (MF-04). Never stored in Git.",
      "properties": {
        "conditions": {
          "description": "Reconciler condition transitions.",
          "items": {
            "$ref": "#/definitions/Condition"
          },
          "type": "array"
        },
        "observedRevision": {
          "description": "Git commit revision observed during reconciliation.",
          "type": [
            "string",
            "null"
          ]
        },
        "phase": {
          "anyOf": [
            {
              "$ref": "#/definitions/Phase"
            },
            {
              "type": "null"
            }
          ],
          "description": "Current lifecycle phase."
        }
      },
      "type": "object"
    },
    "SyncMode": {
      "description": "What a sync run does with the synced subtree.",
      "oneOf": [
        {
          "description": "The source keeps winning; local edits show up as drift.",
          "enum": [
            "mirror"
          ],
          "type": "string"
        },
        {
          "description": "Copied once, then detached.",
          "enum": [
            "oneshot"
          ],
          "type": "string"
        }
      ]
    },
    "SyncOrigin": {
      "additionalProperties": false,
      "description": "What a [`SyncSourceSpec`] follows (MF-27).\n\nExactly one of the three members is set. This is the shape the docs print (`source: { git: { … } }`) and, unlike an externally tagged Rust enum, it survives the YAML round-trip without a `!Git` tag.",
      "properties": {
        "bundle": {
          "anyOf": [
            {
              "$ref": "#/definitions/BundleOrigin"
            },
            {
              "type": "null"
            }
          ],
          "description": "A published bundle URL (MF-17)."
        },
        "git": {
          "anyOf": [
            {
              "$ref": "#/definitions/GitOrigin"
            },
            {
              "type": "null"
            }
          ],
          "description": "A remote Git repository, optionally a subtree of it."
        },
        "platformApi": {
          "anyOf": [
            {
              "$ref": "#/definitions/PlatformApiOrigin"
            },
            {
              "type": "null"
            }
          ],
          "description": "Another instance's public resource API (MF-32)."
        }
      },
      "type": "object"
    },
    "SyncSourceSpec": {
      "additionalProperties": false,
      "description": "Desired specification of a [`SyncSource`][crate::kinds::SyncSource] resource (MF-27..MF-32).",
      "properties": {
        "autoMerge": {
          "default": false,
          "description": "Whether a clean sync merge request merges itself (CC-70, MF-29).",
          "type": "boolean"
        },
        "conflictPolicy": {
          "allOf": [
            {
              "$ref": "#/definitions/ConflictPolicy"
            }
          ],
          "description": "What to do when a resource already exists locally (MF-23)."
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/SyncMode"
            }
          ],
          "description": "Mirror the source continuously or copy it once."
        },
        "prune": {
          "default": false,
          "description": "Whether resources missing from the source are deleted; deletions are never implied (CC-19).",
          "type": "boolean"
        },
        "schedule": {
          "allOf": [
            {
              "$ref": "#/definitions/Schedule"
            }
          ],
          "description": "How often the run happens (MF-28)."
        },
        "selector": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Label selector narrowing which resources of the source are synced (MF-10).",
          "type": "object"
        },
        "source": {
          "allOf": [
            {
              "$ref": "#/definitions/SyncOrigin"
            }
          ],
          "description": "Where the resources come from (MF-27)."
        }
      },
      "required": [
        "conflictPolicy",
        "mode",
        "schedule",
        "source"
      ],
      "type": "object"
    }
  },
  "description": "Kubernetes-style resource envelope (MF-01..MF-04).",
  "properties": {
    "apiVersion": {
      "description": "API version (must match [`API_VERSION`]).",
      "type": "string"
    },
    "kind": {
      "description": "Resource kind name (must match [`Kind::KIND`]).",
      "type": "string"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/definitions/ObjectMeta"
        }
      ],
      "description": "Metadata identifying and labeling the resource."
    },
    "spec": {
      "allOf": [
        {
          "$ref": "#/definitions/SyncSourceSpec"
        }
      ],
      "description": "Desired spec state."
    },
    "status": {
      "anyOf": [
        {
          "$ref": "#/definitions/Status"
        },
        {
          "type": "null"
        }
      ],
      "description": "Server-computed status. Stripped during export and serialization."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "ResourceEnvelope_for_SyncSourceSpec",
  "type": "object"
}
