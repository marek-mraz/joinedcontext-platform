{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "Condition": {
      "additionalProperties": false,
      "description": "Status condition entry.",
      "properties": {
        "lastTransitionTime": {
          "description": "Last transition timestamp.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "message": {
          "description": "Human-readable message.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "description": "Machine-readable reason code.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Status value (\"True\", \"False\", \"Unknown\").",
          "type": "string"
        },
        "type": {
          "description": "Type of condition (e.g. Reconciled, Ready).",
          "type": "string"
        }
      },
      "required": [
        "status",
        "type"
      ],
      "type": "object"
    },
    "DataModelRef": {
      "additionalProperties": false,
      "description": "Reference to a DataModel with name and semantic version (DM-33).",
      "properties": {
        "kind": {
          "description": "Optional resource kind (must be `DataModel` if present) (MF-07).",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Referenced DataModel name (DNS-1123 label).",
          "type": "string"
        },
        "version": {
          "description": "Served major version of the referenced model, `\"1\"`, `\"2\"` … (DM-22, DM-33).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "MappingArtifacts": {
      "additionalProperties": false,
      "description": "The two compiled forms of one transformation specification (DM-52).\n\nBoth are committed beside the manifest and both are reviewed, named rather than derived for the same reason DM-02 names a DataModel's artifacts: what runs is what a reviewer approved, not what a compiler would produce now. A Mapping used only in replicate mode may carry the Bloblang alone; live translation and a view Endpoint need the IR.",
      "properties": {
        "bloblang": {
          "description": "The Bloblang mapping Bento runs, relative to the manifest (DM-35, DM-52).",
          "type": [
            "string",
            "null"
          ]
        },
        "gatewayIr": {
          "description": "The gateway mapping IR the Context Gateway interprets, relative to the manifest (DM-51, DM-52, EP-54).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "MappingSpec": {
      "additionalProperties": false,
      "description": "Desired specification of a [`Mapping`][crate::kinds::Mapping] resource (DM-33..DM-42).",
      "properties": {
        "artifacts": {
          "allOf": [
            {
              "$ref": "#/definitions/MappingArtifacts"
            }
          ],
          "default": {},
          "description": "The compiled artifacts committed beside this specification (DM-52)."
        },
        "contextSpaceRef": {
          "description": "Context Space name to which this mapping belongs (PF-09, DM-33).",
          "type": "string"
        },
        "native": {
          "description": "Native Bloblang escape hatch blocks (DM-38).",
          "items": {
            "$ref": "#/definitions/NativeBlock"
          },
          "type": "array"
        },
        "source": {
          "allOf": [
            {
              "$ref": "#/definitions/DataModelRef"
            }
          ],
          "description": "Source data model reference (DM-33)."
        },
        "target": {
          "allOf": [
            {
              "$ref": "#/definitions/DataModelRef"
            }
          ],
          "description": "Target data model reference (DM-33)."
        },
        "tests": {
          "description": "Golden test cases asserting input entity produces expected output entity (DM-39).",
          "items": {
            "$ref": "#/definitions/MappingTest"
          },
          "type": "array"
        },
        "transformation": {
          "description": "Verbatim LinkML-Map TransformationSpecification payload (DM-33)."
        },
        "vocabularyAlignment": {
          "anyOf": [
            {
              "$ref": "#/definitions/VocabularyAlignment"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional SSSOM alignment set used for editor pre-fill and documentation (DM-42)."
        }
      },
      "required": [
        "contextSpaceRef",
        "source",
        "target",
        "tests",
        "transformation"
      ],
      "type": "object"
    },
    "MappingTest": {
      "additionalProperties": false,
      "description": "Golden test case asserting input entity produces expected output entity (DM-39).",
      "properties": {
        "expect": {
          "description": "Path of the expected output example, relative to the manifest (DM-39).",
          "type": "string"
        },
        "input": {
          "description": "Path of the input example, relative to the manifest (DM-39).",
          "type": "string"
        }
      },
      "required": [
        "expect",
        "input"
      ],
      "type": "object"
    },
    "MultiLanguageMap": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Multi-language map keyed by ISO 639-1 two-letter lowercase language codes",
      "propertyNames": {
        "pattern": "^[a-z]{2}$",
        "type": "string"
      },
      "type": "object"
    },
    "NativeBlock": {
      "additionalProperties": false,
      "description": "Native code escape hatch block for logic LinkML-Map cannot express (DM-38).",
      "properties": {
        "language": {
          "allOf": [
            {
              "$ref": "#/definitions/NativeLanguage"
            }
          ],
          "description": "Native language of the code block (DM-37, DM-38)."
        },
        "source": {
          "description": "Source code of the native processor (non-empty, DM-38).",
          "type": "string"
        },
        "targetSlot": {
          "description": "Target slot identifier to which this native block is attached (`^[A-Za-z_][A-Za-z0-9_]*$`, DM-38).",
          "type": "string"
        }
      },
      "required": [
        "language",
        "source",
        "targetSlot"
      ],
      "type": "object"
    },
    "NativeLanguage": {
      "description": "Supported native transformation language (DM-37, DM-38).",
      "oneOf": [
        {
          "description": "Inline Bento Bloblang processor (DM-37, DM-38).",
          "enum": [
            "bloblang"
          ],
          "type": "string"
        }
      ]
    },
    "ObjectMeta": {
      "additionalProperties": false,
      "description": "Metadata envelope attached to every platform resource (MF-02).",
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value annotations for provenance and ownership (MF-08).",
          "type": "object"
        },
        "description": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual description (PF-24)."
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value labels for filtering (MF-10).",
          "type": "object"
        },
        "name": {
          "description": "Resource name (DNS-1123 label).",
          "type": "string"
        },
        "namespace": {
          "description": "Resource namespace (\"org\" or project slug).",
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "anyOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable multilingual title (PF-24)."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Phase": {
      "description": "Lifecycle phase enumeration.",
      "oneOf": [
        {
          "description": "Resource manifest is in draft state.",
          "enum": [
            "Draft"
          ],
          "type": "string"
        },
        {
          "description": "Resource is pending review or approval.",
          "enum": [
            "Pending"
          ],
          "type": "string"
        },
        {
          "description": "Resource deployment is in progress.",
          "enum": [
            "Deploying"
          ],
          "type": "string"
        },
        {
          "description": "Resource is live and active in cluster.",
          "enum": [
            "Live"
          ],
          "type": "string"
        },
        {
          "description": "Reconciliation error.",
          "enum": [
            "Error"
          ],
          "type": "string"
        }
      ]
    },
    "Status": {
      "additionalProperties": false,
      "description": "Server-computed lifecycle and reconciliation status (MF-04). Never stored in Git.",
      "properties": {
        "conditions": {
          "description": "Reconciler condition transitions.",
          "items": {
            "$ref": "#/definitions/Condition"
          },
          "type": "array"
        },
        "observedRevision": {
          "description": "Git commit revision observed during reconciliation.",
          "type": [
            "string",
            "null"
          ]
        },
        "phase": {
          "anyOf": [
            {
              "$ref": "#/definitions/Phase"
            },
            {
              "type": "null"
            }
          ],
          "description": "Current lifecycle phase."
        }
      },
      "type": "object"
    },
    "TypedRef": {
      "additionalProperties": false,
      "description": "Structured typed resource reference (MF-07).",
      "properties": {
        "kind": {
          "description": "Target resource kind.",
          "type": "string"
        },
        "name": {
          "description": "Target resource name.",
          "type": "string"
        },
        "namespace": {
          "description": "Target resource namespace (defaults to caller namespace if omitted).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind",
        "name"
      ],
      "type": "object"
    },
    "VocabularyAlignment": {
      "additionalProperties": false,
      "description": "Vocabulary alignment set backing the editor's mapping pre-fill (DM-42).",
      "properties": {
        "sssomRef": {
          "allOf": [
            {
              "$ref": "#/definitions/TypedRef"
            }
          ],
          "description": "SSSOM mapping set carrying the alignment; never executed as a transformation (DM-42)."
        }
      },
      "required": [
        "sssomRef"
      ],
      "type": "object"
    }
  },
  "description": "Kubernetes-style resource envelope (MF-01..MF-04).",
  "properties": {
    "apiVersion": {
      "description": "API version (must match [`API_VERSION`]).",
      "type": "string"
    },
    "kind": {
      "description": "Resource kind name (must match [`Kind::KIND`]).",
      "type": "string"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/definitions/ObjectMeta"
        }
      ],
      "description": "Metadata identifying and labeling the resource."
    },
    "spec": {
      "allOf": [
        {
          "$ref": "#/definitions/MappingSpec"
        }
      ],
      "description": "Desired spec state."
    },
    "status": {
      "anyOf": [
        {
          "$ref": "#/definitions/Status"
        },
        {
          "type": "null"
        }
      ],
      "description": "Server-computed status. Stripped during export and serialization."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "ResourceEnvelope_for_MappingSpec",
  "type": "object"
}
