{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "Build": {
      "additionalProperties": false,
      "description": "The artifact one build of an `App` published, as the build lane writes it back (AP-13a).",
      "properties": {
        "builtAt": {
          "description": "When the build lane published it.",
          "format": "date-time",
          "type": "string"
        },
        "commit": {
          "description": "The commit of the source the artifact was built from, 7 to 40 lowercase hexadecimal characters.",
          "type": "string"
        },
        "digest": {
          "description": "The artifact's digest, `sha256:` and 64 lowercase hexadecimal characters. The reconciler deploys this and nothing else (AP-72).",
          "type": "string"
        },
        "sdkVersion": {
          "description": "The version of the app SDK the artifact was built against.",
          "type": "string"
        }
      },
      "required": [
        "builtAt",
        "commit",
        "digest",
        "sdkVersion"
      ],
      "type": "object"
    },
    "Condition": {
      "additionalProperties": false,
      "description": "Status condition entry.",
      "properties": {
        "lastTransitionTime": {
          "description": "Last transition timestamp.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "message": {
          "description": "Human-readable message.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "description": "Machine-readable reason code.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Status value (\"True\", \"False\", \"Unknown\").",
          "type": "string"
        },
        "type": {
          "description": "Type of condition (e.g. Reconciled, Ready).",
          "type": "string"
        }
      },
      "required": [
        "status",
        "type"
      ],
      "type": "object"
    },
    "KeyValue": {
      "additionalProperties": false,
      "description": "One `key`/`value` pair, as CIM 009 writes `receiverInfo` and `notifierInfo`.",
      "properties": {
        "key": {
          "description": "The header name.",
          "type": "string"
        },
        "value": {
          "description": "The header value.",
          "type": "string"
        }
      },
      "required": [
        "key",
        "value"
      ],
      "type": "object"
    },
    "MultiLanguageMap": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Multi-language map keyed by ISO 639-1 two-letter lowercase language codes",
      "propertyNames": {
        "pattern": "^[a-z]{2}$",
        "type": "string"
      },
      "type": "object"
    },
    "Notification": {
      "additionalProperties": false,
      "description": "What is sent when the subscription fires (CIM 009 clause 5.2.14).",
      "properties": {
        "attributes": {
          "description": "The attributes the notification carries; every granted one when absent.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "endpoint": {
          "allOf": [
            {
              "$ref": "#/definitions/NotificationEndpoint"
            }
          ],
          "description": "Where it goes."
        },
        "format": {
          "description": "`normalized`, `concise` or `keyValues`, as the specification names them; the broker's default when absent, which is why this is a string and not an enum of ours.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "endpoint"
      ],
      "type": "object"
    },
    "NotificationEndpoint": {
      "additionalProperties": false,
      "description": "Where a notification goes and how it is addressed (CIM 009 clause 5.2.15).",
      "properties": {
        "accept": {
          "description": "The media type the receiver wants; the broker's default when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "receiverInfo": {
          "description": "Headers the receiver needs, values only — a credential belongs in `secretRef`.",
          "items": {
            "$ref": "#/definitions/KeyValue"
          },
          "type": "array"
        },
        "secretRef": {
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "The credential the notification carries, resolved by the reconciler at apply time and never written into the manifest (MF-31, PF-36)."
        },
        "uri": {
          "description": "The address the platform posts to. `https` outside the cluster, `http` only for a Service name inside it.",
          "type": "string"
        }
      },
      "required": [
        "uri"
      ],
      "type": "object"
    },
    "ObjectMeta": {
      "additionalProperties": false,
      "description": "Metadata envelope attached to every platform resource (MF-02).",
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value annotations for provenance and ownership (MF-08).",
          "type": "object"
        },
        "description": {
          "anyOf": [
            {
              "$ref": "#/definitions/Text"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable description: one string, or the legacy map per locale (UI-50, PF-24)."
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value labels for filtering (MF-10).",
          "type": "object"
        },
        "name": {
          "description": "Resource name (DNS-1123 label).",
          "type": "string"
        },
        "namespace": {
          "description": "Resource namespace (\"org\" or project slug).",
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "anyOf": [
            {
              "$ref": "#/definitions/Text"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable title: one string, or the legacy map per locale (UI-50, PF-24)."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Phase": {
      "description": "Lifecycle phase enumeration.",
      "oneOf": [
        {
          "description": "Resource manifest is in draft state.",
          "enum": [
            "Draft"
          ],
          "type": "string"
        },
        {
          "description": "Resource is pending review or approval.",
          "enum": [
            "Pending"
          ],
          "type": "string"
        },
        {
          "description": "Resource deployment is in progress.",
          "enum": [
            "Deploying"
          ],
          "type": "string"
        },
        {
          "description": "Resource is live and active in cluster.",
          "enum": [
            "Live"
          ],
          "type": "string"
        },
        {
          "description": "Reconciliation error.",
          "enum": [
            "Error"
          ],
          "type": "string"
        },
        {
          "description": "The platform holds something the repository does not declare, or no longer holds what it does (CC-21, UI-25). Configuration cannot reach this phase — every component reads it from the repository (CC-72) — so it is a space whose seed entities the broker answers differently, and the two resolutions of UI-26 are what a person does about it.",
          "enum": [
            "Drifted"
          ],
          "type": "string"
        }
      ]
    },
    "Ref": {
      "anyOf": [
        {
          "description": "Bare string resource name.",
          "type": "string"
        },
        {
          "allOf": [
            {
              "$ref": "#/definitions/TypedRef"
            }
          ],
          "description": "Typed reference with kind, name, and optional namespace."
        }
      ],
      "description": "Reference to another resource, either bare name or typed reference (MF-07)."
    },
    "SecretRef": {
      "additionalProperties": false,
      "description": "Reference to a credential stored in SOPS or OpenBao (PF-36).\n\n`deny_unknown_fields` is a security control: it rejects inline `value:` or `password:` fields at parse time, enforcing that secrets never appear in Git or manifest files.",
      "properties": {
        "envVar": {
          "description": "Optional environment variable name for injection into runners.",
          "type": [
            "string",
            "null"
          ]
        },
        "key": {
          "description": "Optional key within secret.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Secret name in secret store.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Status": {
      "additionalProperties": false,
      "description": "Server-computed lifecycle and reconciliation status (MF-04). Never stored in Git.",
      "properties": {
        "build": {
          "anyOf": [
            {
              "$ref": "#/definitions/Build"
            },
            {
              "type": "null"
            }
          ],
          "description": "What the build lane published for an `App`, and the only place its artifact is named (AP-13a). Written back by the build lane in the commit that publishes the artifact; an App without it renders no pod, and no other principal may set it (AP-73)."
        },
        "conditions": {
          "description": "Reconciler condition transitions.",
          "items": {
            "$ref": "#/definitions/Condition"
          },
          "type": "array"
        },
        "observedRevision": {
          "description": "Git commit revision observed during reconciliation.",
          "type": [
            "string",
            "null"
          ]
        },
        "phase": {
          "anyOf": [
            {
              "$ref": "#/definitions/Phase"
            },
            {
              "type": "null"
            }
          ],
          "description": "Current lifecycle phase."
        }
      },
      "type": "object"
    },
    "SubscriptionSpec": {
      "additionalProperties": false,
      "description": "Desired specification of a `Subscription` resource (CC-72, DS-16).",
      "properties": {
        "contextSpaceRef": {
          "allOf": [
            {
              "$ref": "#/definitions/Ref"
            }
          ],
          "description": "The Context Space whose broker holds the subscription. It is written into this space's tenant and no other (SP-08, SP-09)."
        },
        "description": {
          "description": "Why it exists, for whoever reads the repository later.",
          "type": [
            "string",
            "null"
          ]
        },
        "entities": {
          "description": "What is watched. Empty means \"every type\", which is only allowed together with `watchedAttributes`.",
          "items": {
            "$ref": "#/definitions/WatchedEntity"
          },
          "type": "array"
        },
        "expiresAt": {
          "description": "When the subscription stops, if it is not open-ended.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "geoQ": {
          "description": "The area an entity must be in, as a `geoQ` (`georel`, `geometry`, `coordinates`).",
          "type": [
            "string",
            "null"
          ]
        },
        "isActive": {
          "default": true,
          "description": "Whether the broker delivers; a subscription written with `isActive: false` is declared and silent, which is how one is parked without deleting it.",
          "type": "boolean"
        },
        "notification": {
          "allOf": [
            {
              "$ref": "#/definitions/Notification"
            }
          ],
          "description": "Where the notifications go."
        },
        "q": {
          "description": "The condition an entity must satisfy, in the query language of CIM 009 clause 4.9.",
          "type": [
            "string",
            "null"
          ]
        },
        "subscriptionName": {
          "description": "What a person calls it in the Portal.",
          "type": [
            "string",
            "null"
          ]
        },
        "throttling": {
          "description": "The shortest interval between two notifications, in seconds.",
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "watchedAttributes": {
          "description": "The attributes whose change fires the subscription; any attribute when empty.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "contextSpaceRef",
        "notification"
      ],
      "type": "object"
    },
    "Text": {
      "anyOf": [
        {
          "description": "The form every new manifest writes.",
          "type": "string"
        },
        {
          "allOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            }
          ],
          "description": "The legacy `{locale: text}` map."
        }
      ],
      "description": "A human label or description (UI-50): one plain string in the author's language. The legacy language map is still read, through release `v0.9`, and resolves to one string."
    },
    "TypedRef": {
      "additionalProperties": false,
      "description": "Structured typed resource reference (MF-07).",
      "properties": {
        "kind": {
          "description": "Target resource kind.",
          "type": "string"
        },
        "name": {
          "description": "Target resource name.",
          "type": "string"
        },
        "namespace": {
          "description": "Target resource namespace (defaults to caller namespace if omitted).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind",
        "name"
      ],
      "type": "object"
    },
    "WatchedEntity": {
      "additionalProperties": false,
      "description": "One selector of the entities a subscription watches (CIM 009 clause 5.2.12).\n\nNot `policy::EntitySelector`, which requires a type: a subscription may watch one entity by its URN, and a grant may not be written that loosely.\n\nEither a type, an id, or an id pattern; the broker's own shape, so a query written against the API and a subscription written in a manifest select with the same words.",
      "properties": {
        "id": {
          "description": "One entity, by its URN.",
          "type": [
            "string",
            "null"
          ]
        },
        "idPattern": {
          "description": "Every entity whose id matches this anchored regular expression.",
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "description": "The entity type, as the model names it.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    }
  },
  "description": "Kubernetes-style resource envelope (MF-01..MF-04).",
  "properties": {
    "apiVersion": {
      "description": "API version: [`API_VERSION`], or a version [`serves`] accepts for this kind.",
      "type": "string"
    },
    "kind": {
      "description": "Resource kind name (must match [`Kind::KIND`]).",
      "type": "string"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/definitions/ObjectMeta"
        }
      ],
      "description": "Metadata identifying and labeling the resource."
    },
    "spec": {
      "allOf": [
        {
          "$ref": "#/definitions/SubscriptionSpec"
        }
      ],
      "description": "Desired spec state."
    },
    "status": {
      "anyOf": [
        {
          "$ref": "#/definitions/Status"
        },
        {
          "type": "null"
        }
      ],
      "description": "Server-computed status. Stripped during export and serialization."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "ResourceEnvelope_for_SubscriptionSpec",
  "type": "object"
}
