{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "Build": {
      "additionalProperties": false,
      "description": "The artifact one build of an `App` published, as the build lane writes it back (AP-13a).",
      "properties": {
        "builtAt": {
          "description": "When the build lane published it.",
          "format": "date-time",
          "type": "string"
        },
        "commit": {
          "description": "The commit of the source the artifact was built from, 7 to 40 lowercase hexadecimal characters.",
          "type": "string"
        },
        "digest": {
          "description": "The artifact's digest, `sha256:` and 64 lowercase hexadecimal characters. The reconciler deploys this and nothing else (AP-72).",
          "type": "string"
        },
        "sdkVersion": {
          "description": "The version of the app SDK the artifact was built against.",
          "type": "string"
        }
      },
      "required": [
        "builtAt",
        "commit",
        "digest",
        "sdkVersion"
      ],
      "type": "object"
    },
    "BundleFile": {
      "additionalProperties": false,
      "description": "One file of a bundle and the checksum a transfer is verified against (MF-42).",
      "properties": {
        "path": {
          "description": "Path inside the bundle, as the archive carries it.",
          "type": "string"
        },
        "sha256": {
          "description": "Lowercase hexadecimal SHA-256 of the file's bytes as exported.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "sha256"
      ],
      "type": "object"
    },
    "BundleItem": {
      "additionalProperties": false,
      "description": "One resource listed in a [`BundleSpec`] index.",
      "properties": {
        "kind": {
          "description": "Manifest kind of the exported resource; must be a kind this crate knows.",
          "type": "string"
        },
        "name": {
          "description": "`metadata.name` of the resource.",
          "type": "string"
        },
        "namespace": {
          "description": "Namespace of the resource, absent for organization-scoped kinds.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "Path of the manifest inside the bundle archive.",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "name",
        "path"
      ],
      "type": "object"
    },
    "BundleSchemas": {
      "additionalProperties": false,
      "description": "The schemas of a complete export: one per kind in the bundle, and the LinkML source and JSON Schema of every `DataModel` it holds (MF-41).",
      "properties": {
        "kinds": {
          "additionalProperties": true,
          "description": "Kind name to its JSON Schema (draft-07).",
          "type": "object"
        },
        "models": {
          "additionalProperties": true,
          "description": "DataModel name to `{linkml, jsonSchema}`; a member is absent when neither the repository nor Model Tools could produce it.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "BundleSpec": {
      "additionalProperties": false,
      "description": "Desired specification of a [`Bundle`][crate::kinds::Bundle] download index (MF-17, MF-18).\n\nA Bundle is generated on download and never stored in the repository; it travels inside the archive so the import wizard knows what it received and from where.",
      "properties": {
        "exportedAt": {
          "description": "When the bundle was produced.",
          "format": "date-time",
          "type": "string"
        },
        "exportedBy": {
          "description": "Who produced it (a user or ServiceAccount name).",
          "type": "string"
        },
        "files": {
          "description": "The SHA-256 of every file the bundle holds, over the bytes as exported (MF-42). An import compares what it wrote against these, so a transfer between instances is verified before the source is deleted. A bundle from an older exporter carries none, and an import then reports nothing verified rather than reporting everything equal.",
          "items": {
            "$ref": "#/definitions/BundleFile"
          },
          "type": "array"
        },
        "items": {
          "description": "The exported resources.",
          "items": {
            "$ref": "#/definitions/BundleItem"
          },
          "type": "array"
        },
        "nativeFiles": {
          "description": "Native files carried next to their envelopes (`bento.yaml`, `*.linkml.yaml`) (MF-17).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "omitted": {
          "default": 0,
          "description": "How many resources the caller was not allowed to read; names are never disclosed (MF-18).",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "readme": {
          "description": "What the bundle holds, in prose: the text of the `README.md` a complete export carries at its root, so a stream export says the same thing as an archive (MF-41).",
          "type": [
            "string",
            "null"
          ]
        },
        "schemas": {
          "anyOf": [
            {
              "$ref": "#/definitions/BundleSchemas"
            },
            {
              "type": "null"
            }
          ],
          "description": "The schemas a complete export carries beside its manifests (MF-41)."
        },
        "sourceInstance": {
          "description": "Base URL of the instance it came from, so a re-import can be traced (MF-25).",
          "type": [
            "string",
            "null"
          ]
        },
        "sourceRevision": {
          "description": "Git revision the export was taken at, 7 to 40 lowercase hexadecimal characters.",
          "type": "string"
        }
      },
      "required": [
        "exportedAt",
        "exportedBy",
        "items",
        "sourceRevision"
      ],
      "type": "object"
    },
    "Condition": {
      "additionalProperties": false,
      "description": "Status condition entry.",
      "properties": {
        "lastTransitionTime": {
          "description": "Last transition timestamp.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "message": {
          "description": "Human-readable message.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "description": "Machine-readable reason code.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Status value (\"True\", \"False\", \"Unknown\").",
          "type": "string"
        },
        "type": {
          "description": "Type of condition (e.g. Reconciled, Ready).",
          "type": "string"
        }
      },
      "required": [
        "status",
        "type"
      ],
      "type": "object"
    },
    "MultiLanguageMap": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Multi-language map keyed by ISO 639-1 two-letter lowercase language codes",
      "propertyNames": {
        "pattern": "^[a-z]{2}$",
        "type": "string"
      },
      "type": "object"
    },
    "ObjectMeta": {
      "additionalProperties": false,
      "description": "Metadata envelope attached to every platform resource (MF-02).",
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value annotations for provenance and ownership (MF-08).",
          "type": "object"
        },
        "description": {
          "anyOf": [
            {
              "$ref": "#/definitions/Text"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable description: one string, or the legacy map per locale (UI-50, PF-24)."
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Key-value labels for filtering (MF-10).",
          "type": "object"
        },
        "name": {
          "description": "Resource name (DNS-1123 label).",
          "type": "string"
        },
        "namespace": {
          "description": "Resource namespace (\"org\" or project slug).",
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "anyOf": [
            {
              "$ref": "#/definitions/Text"
            },
            {
              "type": "null"
            }
          ],
          "description": "Human-readable title: one string, or the legacy map per locale (UI-50, PF-24)."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "Phase": {
      "description": "Lifecycle phase enumeration.",
      "oneOf": [
        {
          "description": "Resource manifest is in draft state.",
          "enum": [
            "Draft"
          ],
          "type": "string"
        },
        {
          "description": "Resource is pending review or approval.",
          "enum": [
            "Pending"
          ],
          "type": "string"
        },
        {
          "description": "Resource deployment is in progress.",
          "enum": [
            "Deploying"
          ],
          "type": "string"
        },
        {
          "description": "Resource is live and active in cluster.",
          "enum": [
            "Live"
          ],
          "type": "string"
        },
        {
          "description": "Reconciliation error.",
          "enum": [
            "Error"
          ],
          "type": "string"
        },
        {
          "description": "The platform holds something the repository does not declare, or no longer holds what it does (CC-21, UI-25). Configuration cannot reach this phase — every component reads it from the repository (CC-72) — so it is a space whose seed entities the broker answers differently, and the two resolutions of UI-26 are what a person does about it.",
          "enum": [
            "Drifted"
          ],
          "type": "string"
        }
      ]
    },
    "Status": {
      "additionalProperties": false,
      "description": "Server-computed lifecycle and reconciliation status (MF-04). Never stored in Git.",
      "properties": {
        "build": {
          "anyOf": [
            {
              "$ref": "#/definitions/Build"
            },
            {
              "type": "null"
            }
          ],
          "description": "What the build lane published for an `App`, and the only place its artifact is named (AP-13a). Written back by the build lane in the commit that publishes the artifact; an App without it renders no pod, and no other principal may set it (AP-73)."
        },
        "conditions": {
          "description": "Reconciler condition transitions.",
          "items": {
            "$ref": "#/definitions/Condition"
          },
          "type": "array"
        },
        "observedRevision": {
          "description": "Git commit revision observed during reconciliation.",
          "type": [
            "string",
            "null"
          ]
        },
        "phase": {
          "anyOf": [
            {
              "$ref": "#/definitions/Phase"
            },
            {
              "type": "null"
            }
          ],
          "description": "Current lifecycle phase."
        }
      },
      "type": "object"
    },
    "Text": {
      "anyOf": [
        {
          "description": "The form every new manifest writes.",
          "type": "string"
        },
        {
          "allOf": [
            {
              "$ref": "#/definitions/MultiLanguageMap"
            }
          ],
          "description": "The legacy `{locale: text}` map."
        }
      ],
      "description": "A human label or description (UI-50): one plain string in the author's language. The legacy language map is still read, through release `v0.9`, and resolves to one string."
    }
  },
  "description": "Kubernetes-style resource envelope (MF-01..MF-04).",
  "properties": {
    "apiVersion": {
      "description": "API version (must match [`API_VERSION`]).",
      "type": "string"
    },
    "kind": {
      "description": "Resource kind name (must match [`Kind::KIND`]).",
      "type": "string"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/definitions/ObjectMeta"
        }
      ],
      "description": "Metadata identifying and labeling the resource."
    },
    "spec": {
      "allOf": [
        {
          "$ref": "#/definitions/BundleSpec"
        }
      ],
      "description": "Desired spec state."
    },
    "status": {
      "anyOf": [
        {
          "$ref": "#/definitions/Status"
        },
        {
          "type": "null"
        }
      ],
      "description": "Server-computed status. Stripped during export and serialization."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "ResourceEnvelope_for_BundleSpec",
  "type": "object"
}
